import { randomString } from "../../support/API/helper";

const email = `esosa${randomString}@tsi.com`;

export const user = {
    name: 'esosa oghagbon',
    email,
    gender: 'female',
    status: 'active'
};